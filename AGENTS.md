# AGENTS.md

## Repository overview

The project is divided into different parts (`docs/`, `firmware/`, `pcb/`, `web/`, etc.).
Each has their own `AGENTS.md` that needs to be respected and `mise.toml` that defines the tools needed.
